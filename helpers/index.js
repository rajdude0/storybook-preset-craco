const { getModulePath } = require("./getModulePath");
const { processCraConfig } = require("./processCraConfig");
const { mergePlugins } = require("./mergePlugins");

module.exports = {
		getModulePath,
		processCraConfig,
		mergePlugins
}
